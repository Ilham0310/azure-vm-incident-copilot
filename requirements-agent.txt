# Azure VM Incident Copilot - Agent Dependencies
# Install with: pip install -r requirements-agent.txt

# Azure SDK dependencies for telemetry collection
azure-identity>=1.15.0
azure-mgmt-resourcegraph>=8.0.0
azure-monitor-query>=1.3.0

# Scheduler for periodic execution
apscheduler>=3.10.0

# Include base runtime dependencies
-r requirements.txt
